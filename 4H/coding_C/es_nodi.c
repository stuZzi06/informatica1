#include <stdio.h>

typedef struct nodo
{
    int info;
    nodo *next;
};

void inserisci_testa(struct nodo**, int);

int main()
{
    struct node* testa = NULL;
}

void insersci_testa (struct nodo**, int  new_testa)
{
    struct nodo* nuovo = (struct nodo*) malloc (sizeof(struct nodo));
    if(nuovo = NULL)
    {
        printf("error 404")
    }
}
